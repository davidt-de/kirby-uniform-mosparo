---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - composer.json
  - .gitignore
  - src/
  - config/
autonomous: true
requirements:
  - INFRA-01
  - INFRA-02
  - INFRA-03
must_haves:
  truths:
    - "Composer package is valid and installable"
    - "PSR-4 autoloading resolves classes from src/ directory"
    - "Plugin declares Kirby 4.x+ compatibility"
    - "composer.lock is excluded from git"
  artifacts:
    - path: "composer.json"
      provides: "Package definition, PSR-4 autoloading, dependencies"
      contains: ["name", "type: kirby-plugin", "autoload.psr-4", "require.php: '>=8.0'"]
    - path: ".gitignore"
      provides: "Git exclusions"
      contains: ["/vendor/", "/composer.lock"]
    - path: "src/"
      provides: "PHP source directory"
      min_files: 1
    - path: "config/"
      provides: "Configuration directory"
  key_links:
    - from: "composer.json"
      to: "src/"
      via: "PSR-4 autoload mapping"
      pattern: '"Uniform\\\\Mosparo\\\\": "src/"'
---

<objective>
Set up the foundational project structure with Composer configuration, PSR-4 autoloading, and git exclusions. This establishes the package as a distributable Kirby plugin.

Purpose: Creates the scaffolding that enables installation via `composer require` and automatic Kirby plugin registration.
Output: composer.json, .gitignore, src/ and config/ directories
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md

## Key Decisions from CONTEXT.md
- Directory: src/, tests/, config/ - clean separation
- Package name: vendor/plugin-name format (standard Packagist)
- Kirby compatibility: Kirby 4.x+ only (PHP 8.0+)
- Package type: kirby-plugin for Kirby's official plugin installer support
- composer.lock: ignored via .gitignore
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create composer.json with PSR-4 autoloading</name>
  <files>composer.json</files>
  <action>
Create composer.json with:
1. Package name following pattern: getkirby-uniform/mosparo (vendor/plugin-name format)
2. Type: kirby-plugin (enables Kirby's plugin installer)
3. PSR-4 autoload mapping: "Uniform\\Mosparo\\": "src/"
4. PHP requirement: >=8.0 (Kirby 4.x+ baseline)
5. Dependencies:
   - mzur/kirby-uniform: ^5.0 (base plugin)
   - mosparo/php-api-client: ^1.0 (API client - will be used in Phase 2)
6. Scripts section: Define "test" command placeholder for Phase 2
7. Minimum stability: stable

Package description should reference: "Mosparo spam protection for Kirby Uniform forms"
License: MIT (standard for Kirby plugins)

NOTE: Do NOT commit composer.lock (library best practice per CONTEXT.md).
  </action>
  <verify>
    <automated>composer validate --strict</automated>
    <manual>Check output shows no errors or warnings</manual>
  </verify>
  <done>composer.json passes validation with no errors, contains all required fields</done>
</task>

<task type="auto">
  <name>Task 2: Create .gitignore with standard exclusions</name>
  <files>.gitignore</files>
  <action>
Create .gitignore excluding:
1. /vendor/ - Composer dependencies (never commit)
2. /composer.lock - Lock file (libraries shouldn't commit this per CONTEXT.md)
3. /tests/coverage/ - Test coverage reports
4. .phpunit.result.cache - PHPUnit cache
5. .php-cs-fixer.cache - CS-Fixer cache
6. .idea/ and .vscode/ - IDE directories (optional but helpful)
7. .DS_Store - macOS files

Keep it minimal and focused on build artifacts and dependencies.
  </action>
  <verify>
    <automated>test -f .gitignore && grep -q "composer.lock" .gitignore && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>.gitignore exists and excludes vendor/, composer.lock, and cache files</done>
</task>

<task type="auto">
  <name>Task 3: Create directory structure with placeholder files</name>
  <files>
    - src/
    - src/MosparoPlugin.php
    - config/
  </files>
  <action>
Create directory structure:
1. src/ - PHP source classes (PSR-4 mapped)
2. config/ - Configuration files for tools (PHP-CS-Fixer, PHPStan, etc.)

Create placeholder src/MosparoPlugin.php:
- Namespace: Uniform\Mosparo
- Minimal class that will be expanded in Plan 03
- Include class-level docblock with @package annotation
- Purpose: Verify PSR-4 autoloading works

Directory structure should be:
```
project-root/
├── composer.json
├── .gitignore
├── src/
│   └── MosparoPlugin.php
└── config/
```
  </action>
  <verify>
    <automated>composer install --no-interaction && composer dump-autoload && php -r "require 'vendor/autoload.php'; new Uniform\Mosparo\MosparoPlugin();"</automated>
  </verify>
  <done>Directories created, autoloading resolves MosparoPlugin class without errors</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `composer validate --strict` - must pass
2. Run `composer install` - must complete without errors
3. Run `php -r "require 'vendor/autoload.php'; var_dump(class_exists('Uniform\Mosparo\MosparoPlugin'));"` - must output true
4. Verify .gitignore excludes vendor/ and composer.lock
</verification>

<success_criteria>
- composer.json is valid and passes `composer validate --strict`
- PSR-4 autoloading correctly maps Uniform\Mosparo\ namespace to src/ directory
- composer.lock is excluded from git via .gitignore
- Plugin declares PHP 8.0+ and Kirby 4.x+ compatibility
- Directory structure (src/, config/) is in place
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
