---
phase: 01-foundation
plan: 03
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - index.php
  - src/MosparoPlugin.php
  - src/Guards/MosparoGuard.php
autonomous: true
requirements:
  - INFRA-02
  - INFRA-03
must_haves:
  truths:
    - "Plugin auto-registers with Kirby when placed in site/plugins/"
    - "Kirby recognizes the plugin via index.php and composer.json"
    - "Plugin loads without errors on Kirby 4.x+"
    - "MosparoGuard class extends Uniform Guard base"
  artifacts:
    - path: "index.php"
      provides: "Kirby plugin entry point"
      contains: ["Kirby::plugin", "Uniform\\Mosparo\\MosparoPlugin"]
    - path: "src/MosparoPlugin.php"
      provides: "Main plugin class with registration"
      min_lines: 20
    - path: "src/Guards/MosparoGuard.php"
      provides: "Guard implementation stub"
      contains: ["extends Guard", "perform("]
  key_links:
    - from: "index.php"
      to: "src/MosparoPlugin.php"
      via: "require autoload + Kirby::plugin"
      pattern: "require.*autoload|Kirby::plugin"
    - from: "src/MosparoGuard.php"
      to: "Uniform\\Guards\\Guard"
      via: "class extension"
      pattern: "extends Guard"
---

<objective>
Implement Kirby plugin registration and create the MosparoGuard stub. Establishes the plugin as a first-class Kirby citizen that auto-registers and integrates with Uniform.

Purpose: Enables automatic plugin loading and provides the foundation for spam protection Guards.
Output: index.php entry point, MosparoPlugin class, MosparoGuard stub extending Uniform Guard
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

## Key Decisions from CONTEXT.md
- Registration approach: index.php - traditional Kirby plugin entry point
- Extension points: Guards only for Phase 1 - MosparoGuard class for form validation
- Loading mechanism: Auto-load via Composer autoloader
- Include basic template helpers in Phase 1

## Dependencies
- Plan 01-01 must complete first (Composer setup with autoloading)
- Requires mzur/kirby-uniform package for Guard base class
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create index.php Kirby plugin entry point</name>
  <files>index.php</files>
  <action>
Create index.php as the Kirby plugin entry point:

1. Load Composer autoloader: require __DIR__ . '/vendor/autoload.php'
2. Import MosparoPlugin class: use Uniform\Mosparo\MosparoPlugin
3. Register plugin with Kirby::plugin():
   - Plugin name: 'getkirby-uniform/mosparo' (matching composer package name)
   - Pass MosparoPlugin::register() or similar method
4. Follow Kirby plugin best practices:
   - Check if Kirby class exists (defensive coding)
   - Return early if not in Kirby context
5. Add file-level docblock explaining this is the plugin entry point

The index.php should be minimal - most logic goes in MosparoPlugin class.
Example pattern from other Kirby plugins:
```php
<?php

require_once __DIR__ . '/vendor/autoload.php';

use Kirby\Cms\Kirby;
use Uniform\Mosparo\MosparoPlugin;

if (!class_exists(Kirby::class)) {
    return;
}

Kirby::plugin('getkirby-uniform/mosparo', MosparoPlugin::register());
```
  </action>
  <verify>
    <automated>php -l index.php && grep -q "require.*vendor/autoload.php" index.php && echo "OK" || echo "FAIL"</automated>
  </verify>
  <done>index.php exists, syntax is valid, loads autoloader and registers with Kirby</done>
</task>

<task type="auto">
  <name>Task 2: Implement MosparoPlugin class with registration logic</name>
  <files>src/MosparoPlugin.php</files>
  <action>
Expand src/MosparoPlugin.php with plugin registration logic:

1. Class structure:
   - Namespace: Uniform\Mosparo
   - Class: MosparoPlugin
   - Static method: register() returns array of plugin extensions

2. Plugin extensions to register:
   - 'guards' => ['mosparo' => MosparoGuard::class]
   - 'options' => default configuration array (empty for now, expanded in Phase 2)
   - 'translations' => language files path (prepared for Phase 3)
   - 'snippets' => template helpers path (prepared for Phase 3)

3. Method signature:
   ```php
   public static function register(): array
   ```

4. Include proper PHPDoc:
   - @package Uniform\Mosparo
   - @author [from composer.json]
   - @license MIT

5. Add basic validation:
   - Check if Uniform is installed (defensive)
   - Throw helpful exception if dependencies missing

This replaces the placeholder from Plan 01 with full implementation.
  </action>
  <verify>
    <automated>php -r "require 'vendor/autoload.php'; \$ext = Uniform\Mosparo\MosparoPlugin::register(); var_dump(isset(\$ext['guards']));"</automated>
  </verify>
  <done>MosparoPlugin class returns extension array with guards key, no syntax errors</done>
</task>

<task type="auto">
  <name>Task 3: Create MosparoGuard stub extending Uniform Guard</name>
  <files>src/Guards/MosparoGuard.php</files>
  <action>
Create src/Guards/MosparoGuard.php as stub for Phase 2:

1. Namespace: Uniform\Mosparo\Guards
2. Class: MosparoGuard extends Uniform\Guards\Guard
3. Required method:
   - protected function perform(): void
   - Currently empty or throws exception: "Not implemented - Phase 2"
4. Include PHPDoc:
   - @see https://mosparo.io/docs for API reference
   - @package Uniform\Mosparo\Guards
   - @extends Guard
5. Use statements:
   - use Uniform\Guards\Guard
   - use Uniform\Form\Form (for type hints)

Structure:
```php
<?php

namespace Uniform\Mosparo\Guards;

use Uniform\Guards\Guard;

/**
 * Mosparo spam protection Guard for Kirby Uniform.
 *
 * @package Uniform\Mosparo\Guards
 */
class MosparoGuard extends Guard
{
    /**
     * Perform the verification.
     *
     * @throws \Exception Stub - implementation in Phase 2
     */
    protected function perform(): void
    {
        throw new \RuntimeException('MosparoGuard implementation pending Phase 2');
    }
}
```

This creates the class structure that will be filled in during Phase 2.
  </action>
  <verify>
    <automated>php -r "require 'vendor/autoload.php'; \$r = new ReflectionClass('Uniform\Mosparo\Guards\MosparoGuard'); var_dump(\$r->isSubclassOf('Uniform\Guards\Guard'));"</automated>
  </verify>
  <done>MosparoGuard class exists, extends Uniform Guard, has perform() method</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `php -l index.php` - syntax check
2. Run `php -l src/MosparoPlugin.php` - syntax check
3. Run `php -l src/Guards/MosparoGuard.php` - syntax check
4. Verify autoloading: `php -r "require 'vendor/autoload.php'; new Uniform\Mosparo\Guards\MosparoGuard();"`
5. Check MosparoGuard extends Uniform Guard via reflection
6. Verify index.php registers plugin with correct name pattern
</verification>

<success_criteria>
- index.php exists and loads vendor/autoload.php
- MosparoPlugin::register() returns plugin extension array
- MosparoGuard extends Uniform\Guards\Guard base class
- All PHP files pass syntax validation (php -l)
- PSR-4 autoloading correctly loads all classes
- Plugin follows Kirby plugin naming convention: vendor/plugin-name
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-03-SUMMARY.md`
</output>
