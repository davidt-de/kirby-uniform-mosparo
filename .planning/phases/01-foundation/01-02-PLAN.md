---
phase: 01-foundation
plan: 02
type: execute
wave: 2
depends_on:
  - 01-01
files_modified:
  - phpunit.xml
  - tests/
  - composer.json
autonomous: true
requirements:
  - INFRA-04
must_haves:
  truths:
    - "PHPUnit configuration is valid and loadable"
    - "Tests run with `composer test` command"
    - "Tests directory mirrors src/ structure"
    - "Mocking framework is configured for Kirby classes"
  artifacts:
    - path: "phpunit.xml"
      provides: "PHPUnit configuration"
      contains: ["bootstrap", "testsuites", "coverage"]
    - path: "tests/"
      provides: "Test directory structure"
      min_files: 1
    - path: "tests/bootstrap.php"
      provides: "Test bootstrap with autoloading"
  key_links:
    - from: "composer.json"
      to: "phpunit.xml"
      via: "composer test script"
      pattern: '"test": "phpunit"'
    - from: "tests/"
      to: "src/"
      via: "PSR-4 autoloading in bootstrap"
      pattern: "require.*vendor/autoload.php"
---

<objective>
Set up PHPUnit testing infrastructure with configuration, bootstrap, and directory structure. Enables `composer test` command for running unit tests.

Purpose: Provides testing framework for Phase 2 Guard implementation and ensures code quality from day one.
Output: phpunit.xml, tests/ directory with bootstrap.php, example test file
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
@~/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/01-foundation/01-CONTEXT.md
@.planning/phases/01-foundation/01-01-SUMMARY.md

## Key Decisions from CONTEXT.md
- Test structure: tests/ mirrors src/ structure for easy test discovery
- Mocking approach: Use Mockery or PHPUnit mocks for Kirby classes
- Coverage target: No strict target - add tests as needed
- Test command: `composer test` - simple wrapper script
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create phpunit.xml configuration</name>
  <files>phpunit.xml</files>
  <action>
Create phpunit.xml with:
1. XML declaration with UTF-8 encoding
2. Bootstrap: tests/bootstrap.php
3. Test suite named "Unit" pointing to tests/ directory
4. Coverage configuration for src/ directory
5. PHP version requirement check (>=8.0)
6. Colors enabled for CLI output
7. Verbose output for better debugging

Key settings:
- cacheResultFile=".phpunit.result.cache"
- failOnRisky="true"
- failOnWarning="true"
- executionOrder="depends,defects"

Coverage should include:
- src/ directory (main source)
- Exclude: vendor/, tests/, config/
  </action>
  <verify>
    <automated>vendor/bin/phpunit --configuration phpunit.xml --list-tests 2>&1 | head -5</automated>
  </verify>
  <done>phpunit.xml is valid and PHPUnit can load configuration without errors</done>
</task>

<task type="auto">
  <name>Task 2: Create tests directory with bootstrap and example test</name>
  <files>
    - tests/
    - tests/bootstrap.php
    - tests/MosparoPluginTest.php
  </files>
  <action>
Create tests directory structure:

1. tests/bootstrap.php:
   - Require vendor/autoload.php
   - Set up any test constants or environment
   - Configure Mockery (if using) or PHPUnit mocking
   - Minimal - just bootstrap the autoloader

2. tests/MosparoPluginTest.php:
   - Namespace: Uniform\Mosparo\Tests
   - Class: MosparoPluginTest extends PHPUnit\Framework\TestCase
   - One basic test: testPluginCanBeInstantiated()
   - Tests that MosparoPlugin class can be loaded via autoloader
   - This verifies PSR-4 + PHPUnit integration

Directory structure:
```
tests/
├── bootstrap.php
└── MosparoPluginTest.php
```

Keep tests minimal - this is infrastructure only. Full Guard tests come in Phase 2.
  </action>
  <verify>
    <automated>vendor/bin/phpunit --configuration phpunit.xml tests/MosparoPluginTest.php --testdox</automated>
  </verify>
  <done>Test directory created, example test passes, bootstrap loads correctly</done>
</task>

<task type="auto">
  <name>Task 3: Update composer.json with test dependencies and scripts</name>
  <files>composer.json</files>
  <action>
Update composer.json (created in Plan 01):

1. Add require-dev dependencies:
   - phpunit/phpunit: ^10.0 (modern version for PHP 8.0+)
   - mockery/mockery: ^1.6 (for mocking Kirby classes)
   - phpstan/phpstan: ^1.10 (static analysis - optional but recommended)
   - friendsofphp/php-cs-fixer: ^3.0 (code style - optional)

2. Update scripts section:
   - "test": "phpunit --configuration phpunit.xml"
   - "test-coverage": "phpunit --configuration phpunit.xml --coverage-html tests/coverage"
   - "analyze": "phpstan analyse src tests --level=5" (optional)
   - "format": "php-cs-fixer fix" (optional)

3. Ensure autoload-dev includes tests/ for PSR-4:
   - "Uniform\\Mosparo\\Tests\\": "tests/"

Run composer update after modifying to validate changes.
  </action>
  <verify>
    <automated>composer install --no-interaction && composer test</automated>
  </verify>
  <done>composer.json updated, `composer test` runs PHPUnit successfully</done>
</task>

</tasks>

<verification>
After all tasks complete:
1. Run `composer test` - must execute PHPUnit and show passing tests
2. Verify tests/bootstrap.php loads vendor/autoload.php
3. Check that tests/MosparoPluginTest.php exists and passes
4. Verify phpunit.xml has proper configuration
</verification>

<success_criteria>
- `composer test` command runs PHPUnit and displays test results
- PHPUnit configuration (phpunit.xml) is valid XML with proper settings
- tests/ directory exists with bootstrap.php and example test
- Mockery is available via Composer for mocking Kirby classes
- Tests can autoload classes from src/ directory
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-02-SUMMARY.md`
</output>
