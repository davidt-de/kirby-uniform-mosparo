---
phase: 04-documentation-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - LICENSE
  - CHANGELOG.md
autonomous: true
requirements:
  - DOCS-01
must_haves:
  truths:
    - User can follow README to install plugin via Composer
    - README explains what the plugin does and why to use it
    - README includes system requirements
    - README links to further documentation
  artifacts:
    - path: "README.md"
      provides: "Main project documentation with installation and overview"
      min_lines: 150
      sections:
        - Installation
        - Quick Start
        - Requirements
        - Links to other docs
    - path: "LICENSE"
      provides: "MIT license file"
    - path: "CHANGELOG.md"
      provides: "Version history and changes"
  key_links:
    - from: "README.md"
      to: "CHANGELOG.md"
      via: "Changelog link"
    - from: "README.md"
      to: "LICENSE"
      via: "License reference"
---

<objective>
Create the main README.md with installation instructions, project overview, and quick start guide.

Purpose: First impression for users discovering the plugin — must clearly explain what it does, how to install it, and where to find more information.
Output: README.md, LICENSE, and CHANGELOG.md files
</objective>

<execution_context>
@~/.config/opencode/get-shit-done/workflows/execute-plan.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@composer.json
@index.php

**Phase Requirements:**
- DOCS-01: README with installation instructions (Composer)

**Success Criteria:**
1. User can follow README to install plugin via Composer
2. User can copy-paste template examples from docs into their project
3. User can find all configuration options documented with defaults
4. User migrating from reCAPTCHA/hCaptcha/Turnstile has a step-by-step guide
5. Documentation includes troubleshooting section for common errors

**Plugin Facts:**
- Package: getkirby-uniform/mosparo
- Type: kirby-plugin (Kirby's official plugin installer)
- Requires: PHP >=8.0, Kirby Uniform ^5.0
- Supports: Kirby 3.5+, 4.x, 5.x
- Uses Mosparo PHP API client ^1.0
</context>

<tasks>

<task type="auto">
  <name>Create README.md with installation and overview</name>
  <files>README.md</files>
  <action>
Create a comprehensive README.md file with the following sections:

1. **Title & Badges**: Package name, license badge (MIT), PHP version badge (^8.0), Kirby version badge (3.5+|4.x|5.x)

2. **Description**: What this plugin does (Mosparo spam protection for Kirby Uniform forms), why use Mosparo (privacy-friendly, GDPR-compliant, no Google/Cloudflare tracking), key features

3. **Requirements**: PHP 8.0+, Kirby 3.5+ / 4.x / 5.x, Kirby Uniform plugin ^5.0, Mosparo account (mosparo.io or self-hosted)

4. **Installation**:
   - Via Composer: `composer require getkirby-uniform/mosparo`
   - Via Kirby CLI: `kirby plugin:install getkirby-uniform/mosparo`
   - Manual download instructions
   - Note that it's a kirby-plugin type so Kirby's installer handles it automatically

5. **Quick Start**:
   - Configure Mosparo host, UUID, keys in config.php
   - Add guard to Uniform form
   - Add widget to template
   - 3-step minimal example

6. **What is Mosparo?**:
   - Brief explanation of Mosparo (open-source, self-hostable, privacy-focused)
   - Comparison with reCAPTCHA/hCaptcha (GDPR compliance, no external tracking)
   - Link to mosparo.io

7. **Links**: Configuration reference, Usage examples, Migration guide, Troubleshooting, Changelog

Use clear formatting, copy-paste ready code blocks, and friendly but professional tone.
  </action>
  <verify>
    <automated>test -f README.md && grep -q "composer require" README.md && grep -q "getkirby-uniform/mosparo" README.md && wc -l README.md | awk '{print $1 >= 150}'</automated>
    <manual>README covers: installation, requirements, quick start, what is Mosparo, and links to other docs</manual>
  </verify>
  <done>README.md exists with installation instructions, project overview, quick start guide, and links to other documentation</done>
</task>

<task type="auto">
  <name>Create LICENSE file (MIT)</name>
  <files>LICENSE</files>
  <action>
Create a standard MIT LICENSE file.

Use the standard MIT license text with:
- Copyright year: 2025
- Copyright holder: Patrick Schumacher
- Email: hello@patrick-schumacher.de

Match the license declared in composer.json (which is "MIT").
  </action>
  <verify>
    <automated>test -f LICENSE && grep -q "MIT License" LICENSE && grep -q "Patrick Schumacher" LICENSE</automated>
  </verify>
  <done>LICENSE file exists with MIT license and correct copyright information</done>
</task>

<task type="auto">
  <name>Create CHANGELOG.md with version history</name>
  <files>CHANGELOG.md</files>
  <action>
Create a CHANGELOG.md following Keep a Changelog format.

Sections to include:
- Header with project name and changelog format reference
- [Unreleased] section (empty, ready for future changes)
- [1.0.0] - YYYY-MM-DD (current date) section with:
  - Initial release
  - List of features from v1 requirements:
    - Composer installation with PSR-4 autoloading
    - Kirby 3.5+, 4.x, 5.x compatibility
    - Mosparo Guard for Uniform forms
    - Template helpers (mosparo_field(), mosparo_script())
    - Configuration via Kirby options
    - Security hardening with bypass protection
    - German and English translations
    - Privacy-focused GDPR-compliant spam protection

Use semantic versioning format. Each version should have Added/Changed/Fixed/Removed subsections.
  </action>
  <verify>
    <automated>test -f CHANGELOG.md && grep -q "1.0.0" CHANGELOG.md && grep -q "Unreleased" CHANGELOG.md</automated>
  </verify>
  <done>CHANGELOG.md exists with version 1.0.0 release notes following Keep a Changelog format</done>
</task>

</tasks>

<verification>
- README.md exists with installation instructions
- LICENSE exists with MIT license
- CHANGELOG.md exists with version history
- All files are committed to git
</verification>

<success_criteria>
- User can follow README to install plugin via Composer
- README explains what Mosparo is and why to use this plugin
- README includes system requirements (PHP 8.0+, Kirby versions)
- README provides quick start guide
- README links to configuration reference and other docs
- CHANGELOG.md follows Keep a Changelog format
</success_criteria>

<output>
After completion, create `.planning/phases/04-documentation-release/04-01-SUMMARY.md`
</output>
